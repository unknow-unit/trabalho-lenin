import java.util.Scanner;

/**
Escreva um programa que leia um número inteiro (variável CODIGO). Verificar se o código é
igual a 1, igual a 2 ou igual a 3. Caso não seja, apresentar a mensagem “Código inválido”. Ao
ser verificado o código e constatado que é um valor válido, o programa deve verificar cada
código em separado para determinar seu valor por extenso, ou seja, apresentar a mensagem
“um”, ”dois” ou “três”. (Utilizar o comando Switch).
 */
public class Exercicio7 {
    public static void main(String[] Args){
        int CODIGO;
        Scanner leia = new Scanner(System.in); // ADICIONAR IMPORTAÇÃO AUTOMATICAMENTE Ctrl + Shift + I
        
            System.out.print("Digite 1, 2 ou 3: "); // DIGITAR SYSTEM.OUT MAIS RÁPIDO, ESCREVA SOUT DEPOIS APERTE Ctrl + Espaço
            CODIGO = leia.nextInt();
        
                switch(CODIGO){
                    case 1:
                        System.out.println("Um");
                        break;
                    case 2:
                        System.out.println("Dois");
                        break;    
                    case 3:
                        System.out.println("Três");
                        break;
                    
                    default: System.out.println("Número inválido");
                    
                    
                    
                    
                }
        
        
        
        
        
        
        
        
        
        
        
        
        
        
    }
}
